# General application settings (optional, can be empty if all config is profile-specific)

# You can keep these here as universal defaults, or move them to dev/prod profiles if desired.
# spring.jpa.hibernate.ddl-auto=update # Typically overridden by profiles
# spring.jpa.show-sql=true # Typically overridden by profiles
# spring.jpa.properties.hibernate.format_sql=true # Typically overridden by profiles

# The driver class name is usually universal
spring.datasource.driver-class-name=org.postgresql.Driver

# IMPORTANT: Remove any hardcoded database URLs or credentials from here.
# For example, remove the Neon DB URL you previously had here.